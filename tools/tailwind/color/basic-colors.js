/**
 * @returns {Object<string, string>}
 */
function basicColors() {
  return {
    inherit: 'inherit',
    current: 'currentColor',
    transparent: 'transparent',
    black: '#000',
    white: '#fff',
  }
}

module.exports = { basicColors }
